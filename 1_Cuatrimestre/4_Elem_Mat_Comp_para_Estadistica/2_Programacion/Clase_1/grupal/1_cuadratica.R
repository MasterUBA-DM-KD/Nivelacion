ceros_cuadratica <- function(a, b, c){
  delta <- b^2 - 4*a*c
  if (ddelta >= 0) {
    root_1 = (-b - sqrt(delta)) / 2*a
    root_2 = (-b + sqrt(delta)) / 2*a
    raices = (root_1, root_2)
  } else {
    raices = "No roots"
  }
  return(raices)
}

ceros_cuadratica = function(a, b, c){ discriminante \<- if () return(raices) }

decidir_raiz = function(fun, valor){ 
  es_raiz = ...
  return(es_raiz)
}
  
graficar_funcion_y\_valor = function(fun, valor){ \# ... }
    